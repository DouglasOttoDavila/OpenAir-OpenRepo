const genericDescription = `Daily Check-in | General project discussion | Ticket's QA`;

export const dailyTasks = [
    [8, `${genericDescription}`], //monday
    [8, `${genericDescription} | Specialized Retrospective Check-in`], //tuesday
    [8, `${genericDescription}`], //wednesday
    [8, `${genericDescription}`], //thursday
    [8, `${genericDescription}`]  //friday
];